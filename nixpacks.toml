[phases]
[phases.setup]
cmds = [
  "cd backend && npm install",
  "cd ../frontend && npm install"
]

[phases.build]
cmds = [
  "cd frontend && npm run build"
]

[phases.start]
cmd = "cd backend && npm start"